{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Joan Agents Configuration",
  "description": "Configuration file for Joan multi-agent orchestration system",
  "type": "object",
  "required": ["projectId", "projectName", "settings", "agents"],
  "properties": {
    "projectId": {
      "type": "string",
      "format": "uuid",
      "description": "The Joan project UUID this agent system operates on"
    },
    "projectName": {
      "type": "string",
      "minLength": 1,
      "description": "Human-readable project name for display"
    },
    "settings": {
      "type": "object",
      "required": ["pollingIntervalMinutes", "maxIdlePolls"],
      "properties": {
        "model": {
          "type": "string",
          "enum": ["opus", "sonnet", "haiku"],
          "default": "opus",
          "description": "Claude model to use for all agents"
        },
        "pollingIntervalMinutes": {
          "type": "integer",
          "minimum": 1,
          "maximum": 60,
          "default": 10,
          "description": "Minutes between polls when task queue is empty"
        },
        "maxIdlePolls": {
          "type": "integer",
          "minimum": 1,
          "maximum": 100,
          "default": 6,
          "description": "Number of consecutive empty polls before agent shuts down"
        },
        "staleClaimMinutes": {
          "type": "integer",
          "minimum": 10,
          "maximum": 1440,
          "default": 60,
          "description": "Minutes before orphaned dev claims are auto-released (self-healing)"
        },
        "stuckStateMinutes": {
          "type": "integer",
          "minimum": 15,
          "maximum": 1440,
          "default": 120,
          "description": "Minutes before tasks in same workflow state are flagged as stuck"
        },
        "schedulerIntervalSeconds": {
          "type": "integer",
          "minimum": 60,
          "maximum": 3600,
          "default": 300,
          "description": "Seconds between coordinator spawns when using external scheduler (/agents:scheduler)"
        },
        "schedulerStuckTimeoutSeconds": {
          "type": "integer",
          "minimum": 120,
          "maximum": 1800,
          "default": 600,
          "description": "Seconds before external scheduler kills a stuck coordinator (no heartbeat)"
        },
        "schedulerMaxConsecutiveFailures": {
          "type": "integer",
          "minimum": 1,
          "maximum": 10,
          "default": 3,
          "description": "Max consecutive coordinator failures before scheduler stops"
        },
        "pipeline": {
          "type": "object",
          "description": "Strict serial pipeline settings - ensures ONE task flows through Architect→Dev→Review→Ops at a time",
          "properties": {
            "baQueueDraining": {
              "type": "boolean",
              "default": true,
              "description": "Process ALL BA tasks before moving to serial dev pipeline"
            },
            "maxBaTasksPerCycle": {
              "type": "integer",
              "minimum": 1,
              "maximum": 50,
              "default": 10,
              "description": "Maximum BA tasks to drain per dispatch cycle"
            }
          }
        },
        "workerTimeouts": {
          "type": "object",
          "description": "Timeout settings (in minutes) for each worker type",
          "properties": {
            "ba": {
              "type": "integer",
              "minimum": 5,
              "maximum": 60,
              "default": 10,
              "description": "BA worker timeout in minutes"
            },
            "architect": {
              "type": "integer",
              "minimum": 10,
              "maximum": 120,
              "default": 20,
              "description": "Architect worker timeout in minutes"
            },
            "dev": {
              "type": "integer",
              "minimum": 15,
              "maximum": 180,
              "default": 60,
              "description": "Dev worker timeout in minutes"
            },
            "reviewer": {
              "type": "integer",
              "minimum": 10,
              "maximum": 120,
              "default": 20,
              "description": "Reviewer worker timeout in minutes"
            },
            "ops": {
              "type": "integer",
              "minimum": 5,
              "maximum": 60,
              "default": 15,
              "description": "Ops worker timeout in minutes"
            }
          }
        }
      }
    },
    "agents": {
      "type": "object",
      "properties": {
        "businessAnalyst": {
          "type": "object",
          "properties": {
            "enabled": {
              "type": "boolean",
              "default": true,
              "description": "Whether the BA agent is enabled"
            }
          },
          "required": ["enabled"]
        },
        "architect": {
          "type": "object",
          "properties": {
            "enabled": {
              "type": "boolean",
              "default": true,
              "description": "Whether the Architect agent is enabled"
            }
          },
          "required": ["enabled"]
        },
        "ops": {
          "type": "object",
          "properties": {
            "enabled": {
              "type": "boolean",
              "default": true,
              "description": "Whether the Ops agent is enabled"
            }
          },
          "required": ["enabled"]
        },
        "reviewer": {
          "type": "object",
          "properties": {
            "enabled": {
              "type": "boolean",
              "default": true,
              "description": "Whether the Code Reviewer agent is enabled"
            }
          },
          "required": ["enabled"]
        },
        "devs": {
          "type": "object",
          "properties": {
            "enabled": {
              "type": "boolean",
              "default": true,
              "description": "Whether worker agents are enabled"
            },
            "count": {
              "type": "integer",
              "minimum": 1,
              "maximum": 1,
              "default": 1,
              "description": "Number of dev workers (strict serial mode enforces 1)"
            }
          },
          "required": ["enabled", "count"]
        }
      },
      "required": ["businessAnalyst", "architect", "ops", "reviewer", "devs"]
    }
  }
}
